name: CI/CD - MAIN
on:
  workflow_call:
    inputs:
      appType:
        required: true
        type: string
jobs:
  NodeJSBuild:
    if: inputs.appType == 'nodejs'
    runs-on: self-hosted
    steps:
      - run: echo OK
