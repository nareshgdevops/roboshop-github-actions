name: CI/CD - MAIN
on:
  workflow_call:
    inputs:
      appType:
        required: true
        type: string
jobs:
  NodeJSBuild:
    if: inputs.appType == 'nodejs'
    runs-on: self-hosted
    steps:
      - run: npm install
  DEV:
    runs-on: self-hosted
    needs: NodeJSBuild
    steps:
      - run: echo OK