name: CI/CD - MAIN
on:
  workflow_call:
    inputs:
      appType:
        required: true
        type: string
jobs:
  Build:
    uses: nareshgdevops/roboshop-github-actions/.github/workflows/CI-reusable.yml@main
    with:
      appType: nodejs