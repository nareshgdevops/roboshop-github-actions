name: CI/CD - MAIN
on:
  workflow_call
jobs:
  NodeJSBuild:
    if: inputs.appType == 'nodejs'
    runs-on: self-hosted
    steps:
      - run: echo OK
  DEV:
    runs-on: self-hosted
    needs: NodeJSBuild
    steps:
      - run: echo OK
  TEST:
    runs-on: self-hosted
    needs: DEV
    steps:
      - run: echo OK